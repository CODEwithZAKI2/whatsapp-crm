import { Box, Heading, Text } from "@chakra-ui/react";

export default function Templates() {
    return (
        <Box>
            <Heading mb={4}>Templates</Heading>
            <Text>Manage, preview, and test message templates here.</Text>
        </Box>
    );
}
