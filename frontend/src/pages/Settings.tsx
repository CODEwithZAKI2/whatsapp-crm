import { Box, Heading, Text } from "@chakra-ui/react";

export default function Settings() {
    return (
        <Box>
            <Heading mb={4}>Settings</Heading>
            <Text>WhatsApp connection, notifications, backup, etc.</Text>
        </Box>
    );
}
