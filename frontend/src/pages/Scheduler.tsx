import { Box, Heading, Text } from "@chakra-ui/react";

export default function Scheduler() {
    return (
        <Box>
            <Heading mb={4}>Scheduler</Heading>
            <Text>Control, configure, and monitor automation here.</Text>
        </Box>
    );
}
