import { Box, Heading, Text } from "@chakra-ui/react";

export default function Clients() {
    return (
        <Box>
            <Heading mb={4}>Clients</Heading>
            <Text>Manage, search, import/export clients here.</Text>
        </Box>
    );
}
